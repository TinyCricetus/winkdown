# Winkdown

一个基于 SlateJS 的富文本编辑器，支持 Markdown 风格的快捷输入。

## 特性

- 📝 **Markdown 风格快捷键**：通过输入 Markdown 语法快速创建内容
  - `#` 创建标题（支持 H1-H6）
  - `>` 创建引用块
  - ` ``` ` 创建代码块
  - `1.` 创建有序列表
  - `-` 或 `*` 创建无序列表
- 🎨 **文本格式化**：支持粗体、斜体、下划线、行内代码
- 📋 **列表功能**：支持有序/无序列表，使用 Tab 键进行缩进
- ⌨️ **快捷键支持**：
  - `Ctrl/Cmd + B`：粗体
  - `Ctrl/Cmd + I`：斜体
  - `Ctrl/Cmd + U`：下划线
  - `Ctrl/Cmd + ` `：行内代码
  - `Tab`：列表缩进
  - `Shift + Tab`：减少列表缩进

## 安装

```bash
# 使用 pnpm
pnpm install

# 或使用 npm
npm install

# 或使用 yarn
yarn install
```

## 使用

### 开发模式

```bash
pnpm dev
```

### 构建

```bash
pnpm start
```

## 技术栈

- [React](https://react.dev/) - UI 框架
- [Slate](https://www.slatejs.org/) - 富文本编辑器框架
- [TypeScript](https://www.typescriptlang.org/) - 类型安全
- [Webpack](https://webpack.js.org/) - 构建工具

## 项目结构

```
winkdown/
├── src/
│   ├── components/
│   │   ├── winkdown.tsx    # 编辑器主组件
│   │   └── winkdown.css    # 编辑器样式
│   ├── constants.ts        # 类型定义
│   └── index.tsx           # 入口文件
├── public/                 # 静态资源
└── script/                 # 构建脚本
```

## 许可证

ISC
